#include<stdio.h>
int main()
{
	int i,j,k,n;
	scanf("%d",&n);//Enter the size of pyramid
	for(i=1;i<=n;i++)
	{
		for(j=i;j<n;j++)
			printf(" ");
		for(k=0;k<(2*i)-1;k++)
		{
			if(k%2==0)
				printf("*");
			else
				printf(" ");
		}
		printf("\n");
	}
	return 0;
}
		
